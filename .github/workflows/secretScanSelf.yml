# Scans this repository for committed credentials.
#
# The ref is local rather than @main, so a pull request that changes secretScan.yml is checked by
# the version of the workflow it proposes.
name: Secret Scan

on:
  pull_request:

jobs:
  secretScan:
    uses: ./.github/workflows/secretScan.yml

# vim: set ft=yaml:
